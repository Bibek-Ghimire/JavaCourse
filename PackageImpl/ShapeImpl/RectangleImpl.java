package PackageImpl.ShapeImpl;

import PackageImpl.Shape.Rectangle;

public class RectangleImpl implements Rectangle {
    public void Area(int a, int b) {
        System.out.println("The area of Square is " + (a * b));
    }
}
